<?xml version="1.0" encoding="utf-8"?>
<!-- 1. Bungkus semuanya dengan tag <layout> -->
<layout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- 2. Tambahkan blok <data> untuk mendefinisikan variabel ViewModel -->
    <data>
        <variable
            name="viewModel"
            type="com.example.lab_week_13.MovieViewModel" />
    </data>

    <androidx.constraintlayout.widget.ConstraintLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        tools:context=".MainActivity">

        <!-- 3. Gunakan Binding Adapter app:list untuk menghubungkan data -->
        <androidx.recyclerview.widget.RecyclerView
            android:id="@+id/movie_list"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            app:layoutManager="androidx.recyclerview.widget.GridLayoutManager"
            app:layout_constraintBottom_toBottomOf="parent"
            app:layout_constraintTop_toTopOf="parent"
            app:spanCount="2"
            tools:listitem="@layout/view_movie_item"
            app:list="@{viewModel.popularMovies}"/>

    </androidx.constraintlayout.widget.ConstraintLayout>
</layout>
